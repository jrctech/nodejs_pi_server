{
  "name": "nodejs_pi_server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --ignore public/ && nodemon index.js",
    "devRemote": "clear && git pull origin master && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.5"
  },
  "dependencies": {
    "express": "^4.17.1",
    "onoff": "^6.0.1",
    "ws": "^7.3.1"
  }
}
